import validationUtil from './validationUtil'


const formUtil = {
    pwdValidate(formObj,formVal){
        return function(rule,value,callback){
            
        };
    }
}

export default formUtil;